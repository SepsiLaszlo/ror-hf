<p id="notice"><%= notice %></p>

<h1>Hallgatók</h1>

<table class="table">
  <thead>
    <tr>
      <th scope="col">Név</th>
      <th scope="col">Neptun</th>
      <th scope="col">Lehetőségek</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td><%= user.neptun %></td>
        <td><%= link_to 'Profil', user %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New User', new_user_path %>
