<%= form_with(model: course_type) do |form| %>
  <% if course_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course_type.errors.count, "error") %> prohibited this course_type from being saved:</h2>

      <ul>
        <% course_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
